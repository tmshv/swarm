{
    "name": "@tmshv/swarm-mono",
    "private": true,
    "version": "0.1.0",
    "workspaces": [
        "./packages/*",
        "./apps/*",
        "./examples/*"
    ],
    "description": "Swarm monorepo",
    "type": "module",
    "scripts": {
        "dev": "npm run --prefix apps/app dev",
        "build": "npm run --prefix apps/app build",
        "start": "npm run --prefix apps/app start"
    },
    "devDependencies": {
        "@parcel/packager-ts": "^2.11.0",
        "@parcel/transformer-typescript-types": "^2.11.0",
        "typescript": "^5.2.2"
    }
}
